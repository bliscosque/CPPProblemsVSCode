#include <bits/stdc++.h>

using namespace std;

int main() {
    int tc;
    cin >> tc;
    while (tc>0) {
        long long ans=0;
        int n;
        cin >> n;
        vector<string> vs;
        for (int i=0;i<n;i++) {
            string value;
            cin >> value;
            vs.push_back(value);
        }
        if (n==1) {
            cout << 0 << endl;
        }
        else {

        }

        tc--;
    }
    
    return 0;
}